# Copy to apps/api/.dev.vars
# NEVER commit real secrets. GEMINI_API_KEY stays on the Worker only.

BETTER_AUTH_SECRET=generate-a-long-random-string-at-least-32-chars
BETTER_AUTH_URL=http://localhost:8787
GEMINI_API_KEY=
LLM_PROVIDER=mock
